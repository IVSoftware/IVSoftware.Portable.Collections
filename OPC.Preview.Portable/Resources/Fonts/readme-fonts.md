# Fonts

Returning a glyph from an enum requires that the `enum` is declared in the _same assembly_ as the config.json file. For this reason, config files are placed in this folder to match enums like `StdIconRadio`.